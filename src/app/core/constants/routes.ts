import { indexType } from "@core/models/types";

//***********8 */ It does not work, it must be changed *********88
export const ROUTES: indexType = {
    base : '',
    links : {
        base : 'links',
        create : 'create', 
        edit : 'edit'
    }
}